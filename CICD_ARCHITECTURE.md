# 📊 CI/CD Архитектура и Схемы

## 🎯 Общая схема деплоя

```
┌─────────────────────────────────────────────────────────────────┐
│                     РАЗРАБОТКА                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  👨‍💻 Developer                                                   │
│   │                                                              │
│   │ Пишет код в Codespaces/Local                                │
│   │                                                              │
│   ▼                                                              │
│  📝 git add . && git commit -m "..." && git push origin main     │
│                                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Push to GitHub
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     GITHUB                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📦 Repository: isolovyev77/natrium-smm-bot                      │
│   │                                                              │
│   ├─ main branch updated                                        │
│   │                                                              │
│   ▼                                                              │
│  ⚙️  GitHub Actions Workflow Triggered                          │
│   │                                                              │
│   ├─ Event: on push to main                                     │
│   ├─ Runner: ubuntu-latest                                      │
│   ├─ Action: appleboy/ssh-action@v1.0.3                         │
│   │                                                              │
│   └─ Uses Secrets:                                              │
│       • DEPLOY_KEY (private SSH key)                            │
│       • ORACLE_SSH_HOST (VM IP)                                 │
│       • ORACLE_SSH_USER (username)                              │
│       • TELEGRAM_BOT_TOKEN                                      │
│       • YANDEX_* (API credentials)                              │
│                                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ SSH Connection
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     ORACLE VM                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🖥️  Oracle Cloud Virtual Machine                               │
│   │  IP: <ORACLE_SSH_HOST>                                      │
│   │  User: ubuntu/opc                                           │
│   │  Path: /opt/natrium-smm-bot                                 │
│   │                                                              │
│   ▼                                                              │
│  📥 Step 1: git pull origin main                                │
│   │  • Uses Deploy Key (~/.ssh/github_deploy_key)              │
│   │  • Pulls latest code from GitHub                           │
│   │                                                              │
│   ▼                                                              │
│  📝 Step 2: Update .env                                         │
│   │  • Creates/updates .env file                               │
│   │  • Sets TELEGRAM_BOT_TOKEN from GitHub Secret              │
│   │  • Sets YANDEX_FOLDER_ID from GitHub Secret                │
│   │  • Sets YANDEX_AGENT_ID from GitHub Secret                 │
│   │  • Sets YANDEX_API_KEY from GitHub Secret                  │
│   │  • chmod 600 .env (secure permissions)                     │
│   │                                                              │
│   ▼                                                              │
│  📚 Step 3: Install dependencies                                │
│   │  • venv/bin/pip install -r requirements.txt                │
│   │  • Updates all Python packages                             │
│   │                                                              │
│   ▼                                                              │
│  🔄 Step 4: Restart service                                     │
│   │  • sudo systemctl stop natrium-smm-bot                     │
│   │  • sleep 2                                                  │
│   │  • sudo systemctl start natrium-smm-bot                    │
│   │  • sleep 3                                                  │
│   │  • (no password required - sudoers configured)             │
│   │                                                              │
│   ▼                                                              │
│  ✅ Step 5: Verify status                                       │
│   │  • sudo systemctl status natrium-smm-bot                   │
│   │  • Shows status in GitHub Actions logs                     │
│   │                                                              │
│   ▼                                                              │
│  🤖 Bot Running!                                                │
│   │  • Python process: src/telegram_bot.py                     │
│   │  • Managed by systemd                                      │
│   │  • Auto-restart on failure                                 │
│   │  • Logs: /var/log/natrium-smm-bot.log                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 SSH и Deploy Key схема

```
┌────────────────────────────────────────────────────────────┐
│                   GITHUB REPOSITORY                         │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  Settings → Deploy Keys                                     │
│  ┌──────────────────────────────────────────────┐          │
│  │ Oracle VM Deploy Key                         │          │
│  │                                              │          │
│  │ ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAA...      │          │
│  │                                              │          │
│  │ ☐ Allow write access (READ ONLY!)           │          │
│  └──────────────────────────────────────────────┘          │
│                                                             │
│  Settings → Secrets → Actions                               │
│  ┌──────────────────────────────────────────────┐          │
│  │ DEPLOY_KEY (Private)                         │          │
│  │ -----BEGIN OPENSSH PRIVATE KEY-----          │          │
│  │ b3BlbnNzaC1rZXktdjEAAAAABG5vbmUA...         │          │
│  │ -----END OPENSSH PRIVATE KEY-----            │          │
│  └──────────────────────────────────────────────┘          │
│                                                             │
└────────────────────────┬───────────────────────────────────┘
                         │
                         │ GitHub Actions uses DEPLOY_KEY
                         ▼
┌────────────────────────────────────────────────────────────┐
│                   GITHUB ACTIONS                            │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  appleboy/ssh-action@v1.0.3                                 │
│  ├─ host: ${{ secrets.ORACLE_SSH_HOST }}                   │
│  ├─ username: ${{ secrets.ORACLE_SSH_USER }}               │
│  ├─ key: ${{ secrets.DEPLOY_KEY }} ← Uses this!           │
│  └─ script: |                                              │
│       cd /opt/natrium-smm-bot                              │
│       git pull origin main                                  │
│       ...                                                   │
│                                                             │
└────────────────────────┬───────────────────────────────────┘
                         │
                         │ SSH to Oracle VM
                         ▼
┌────────────────────────────────────────────────────────────┐
│                   ORACLE VM                                 │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  ~/.ssh/github_deploy_key (Private Key)                    │
│  ┌──────────────────────────────────────────────┐          │
│  │ -----BEGIN OPENSSH PRIVATE KEY-----          │          │
│  │ b3BlbnNzaC1rZXktdjEAAAAABG5vbmUA...         │          │
│  │ -----END OPENSSH PRIVATE KEY-----            │          │
│  │                                              │          │
│  │ • Created by setup_deploy_key.sh             │          │
│  │ • Used by Git for pull operations            │          │
│  │ • Matches public key in GitHub Deploy Keys   │          │
│  └──────────────────────────────────────────────┘          │
│                                                             │
│  /opt/natrium-smm-bot/.git/config                           │
│  ┌──────────────────────────────────────────────┐          │
│  │ [core]                                       │          │
│  │   sshCommand = ssh -i ~/.ssh/github_deploy_key \        │
│  │                -o IdentitiesOnly=yes         │          │
│  │                                              │          │
│  │ [remote "origin"]                            │          │
│  │   url = git@github.com:isolovyev77/natrium-smm-bot.git  │
│  │   fetch = +refs/heads/*:refs/remotes/origin/*│          │
│  └──────────────────────────────────────────────┘          │
│                                                             │
│  When `git pull` is executed:                               │
│  1. Git uses sshCommand from config                        │
│  2. Connects to github.com with Deploy Key                 │
│  3. Authenticates with public key on GitHub                │
│  4. Pulls latest code (read-only access)                   │
│  5. No password required!                                  │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

---

## 🔄 Сравнение старого и нового workflow

### ❌ Старый workflow (webfactory/ssh-agent)

```yaml
steps:
  - name: Checkout code
    uses: actions/checkout@v4
    
  - name: Setup SSH
    uses: webfactory/ssh-agent@v0.9.0
    with:
      ssh-private-key: ${{ secrets.ORACLE_SSH_KEY }}
    
  - name: Add server to known_hosts
    run: ssh-keyscan -H ${{ secrets.ORACLE_SSH_HOST }} >> ~/.ssh/known_hosts
    
  - name: Deploy
    run: |
      ssh $SSH_USER@$SSH_HOST << 'ENDSSH'
        cd /opt/natrium-smm-bot
        if [ -f .env ]; then cp .env .env.backup; fi
        git fetch origin main
        git reset --hard origin/main
        if [ -f .env.backup ]; then mv .env.backup .env; fi
        source venv/bin/activate
        pip install -r requirements.txt --quiet
        sudo systemctl restart natrium-smm-bot
      ENDSSH
    
  - name: Verify deployment
    if: success()
    run: ssh $SSH_USER@$SSH_HOST "sudo systemctl status natrium-smm-bot"
    
  - name: Show logs
    if: failure()
    run: ssh $SSH_USER@$SSH_HOST "sudo journalctl -u natrium-smm-bot"
```

**Проблемы:**
- ❌ 5 отдельных steps = медленнее
- ❌ .env резервное копирование может сломаться
- ❌ Нет автоматического обновления секретов
- ❌ Сложная настройка SSH
- ❌ Отдельные steps для проверки

---

### ✅ Новый workflow (appleboy/ssh-action)

```yaml
steps:
  - name: 🚀 Deploy to Oracle VM
    uses: appleboy/ssh-action@v1.0.3
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      YANDEX_FOLDER_ID: ${{ secrets.YANDEX_FOLDER_ID }}
      YANDEX_AGENT_ID: ${{ secrets.YANDEX_AGENT_ID }}
      YANDEX_API_KEY: ${{ secrets.YANDEX_API_KEY }}
    with:
      host: ${{ secrets.ORACLE_SSH_HOST }}
      username: ${{ secrets.ORACLE_SSH_USER }}
      key: ${{ secrets.DEPLOY_KEY }}
      envs: TELEGRAM_BOT_TOKEN,YANDEX_FOLDER_ID,YANDEX_AGENT_ID,YANDEX_API_KEY
      script: |
        cd /opt/natrium-smm-bot
        git pull origin main
        
        # Auto-update .env
        update_env_var() {
          key=$1; value=$2
          if [ -n "$value" ]; then
            sed -i "s|^${key}=.*|${key}=${value}|" .env || echo "${key}=${value}" >> .env
          fi
        }
        
        update_env_var "TELEGRAM_BOT_TOKEN" "$TELEGRAM_BOT_TOKEN"
        update_env_var "YANDEX_FOLDER_ID" "$YANDEX_FOLDER_ID"
        update_env_var "YANDEX_AGENT_ID" "$YANDEX_AGENT_ID"
        update_env_var "YANDEX_API_KEY" "$YANDEX_API_KEY"
        chmod 600 .env
        
        venv/bin/pip install -r requirements.txt --quiet
        sudo systemctl restart natrium-smm-bot
        sudo systemctl status natrium-smm-bot --no-pager | head -10
```

**Преимущества:**
- ✅ Один step вместо пяти
- ✅ Автоматическое обновление .env из секретов
- ✅ Встроенная проверка статуса
- ✅ Простая настройка
- ✅ Более надежная работа

---

## 📊 Процесс обновления .env

```
┌─────────────────────────────────────────────────────────────┐
│               GITHUB SECRETS                                 │
├─────────────────────────────────────────────────────────────┤
│  TELEGRAM_BOT_TOKEN = "123456:ABC-DEF..."                    │
│  YANDEX_FOLDER_ID = "b1g..."                                 │
│  YANDEX_AGENT_ID = "agent..."                                │
│  YANDEX_API_KEY = "AQ..."                                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Passed as environment variables
                       ▼
┌─────────────────────────────────────────────────────────────┐
│               GITHUB ACTIONS                                 │
├─────────────────────────────────────────────────────────────┤
│  env:                                                        │
│    TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}     │
│    YANDEX_FOLDER_ID: ${{ secrets.YANDEX_FOLDER_ID }}         │
│    ...                                                       │
│                                                              │
│  with:                                                       │
│    envs: TELEGRAM_BOT_TOKEN,YANDEX_FOLDER_ID,...            │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Variables available in SSH session
                       ▼
┌─────────────────────────────────────────────────────────────┐
│               ORACLE VM - SSH SESSION                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  function update_env_var() {                                 │
│    local key=$1                                              │
│    local value=$2                                            │
│                                                              │
│    if [ -n "$value" ]; then                                  │
│      if grep -q "^${key}=" .env; then                        │
│        # Update existing                                     │
│        sed -i "s|^${key}=.*|${key}=${value}|" .env          │
│      else                                                    │
│        # Add new                                             │
│        echo "${key}=${value}" >> .env                        │
│      fi                                                      │
│    fi                                                        │
│  }                                                           │
│                                                              │
│  update_env_var "TELEGRAM_BOT_TOKEN" "$TELEGRAM_BOT_TOKEN"   │
│  update_env_var "YANDEX_FOLDER_ID" "$YANDEX_FOLDER_ID"       │
│  update_env_var "YANDEX_AGENT_ID" "$YANDEX_AGENT_ID"         │
│  update_env_var "YANDEX_API_KEY" "$YANDEX_API_KEY"           │
│                                                              │
│  chmod 600 .env                                              │
│                                                              │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│               /opt/natrium-smm-bot/.env                      │
├─────────────────────────────────────────────────────────────┤
│  TELEGRAM_BOT_TOKEN=123456:ABC-DEF...                        │
│  YANDEX_FOLDER_ID=b1g...                                     │
│  YANDEX_AGENT_ID=agent...                                    │
│  YANDEX_API_KEY=AQ...                                        │
│                                                              │
│  Permissions: 600 (read/write owner only)                    │
│  Owner: ubuntu:ubuntu                                        │
└─────────────────────────────────────────────────────────────┘
                       │
                       │ Loaded by bot on startup
                       ▼
┌─────────────────────────────────────────────────────────────┐
│               TELEGRAM BOT PROCESS                           │
├─────────────────────────────────────────────────────────────┤
│  from dotenv import load_dotenv                              │
│  load_dotenv()                                               │
│                                                              │
│  TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")        │
│  YANDEX_FOLDER_ID = os.getenv("YANDEX_FOLDER_ID")           │
│  ...                                                         │
│                                                              │
│  ✅ Bot uses updated configuration!                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Безопасность sudoers

```
┌─────────────────────────────────────────────────────────────┐
│               /etc/sudoers.d/natrium-smm-bot                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ubuntu ALL=(ALL) NOPASSWD: \                                │
│    /bin/systemctl restart natrium-smm-bot, \                │
│    /bin/systemctl stop natrium-smm-bot, \                   │
│    /bin/systemctl start natrium-smm-bot, \                  │
│    /bin/systemctl status natrium-smm-bot, \                 │
│    /bin/journalctl -u natrium-smm-bot *                     │
│                                                              │
│  Permissions: 0440 (read-only)                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘

Что это означает:
┌─────────────────────────────────────────────────────────────┐
│  ubuntu                  User who can run commands          │
│  ALL=(ALL)               From any host as any user          │
│  NOPASSWD:               Without entering password          │
│  /bin/systemctl restart  Only these specific commands:      │
│  /bin/systemctl stop       • restart natrium-smm-bot       │
│  /bin/systemctl start      • stop natrium-smm-bot          │
│  /bin/systemctl status     • start natrium-smm-bot         │
│  /bin/journalctl -u        • status natrium-smm-bot        │
│                            • journalctl for this service    │
│                                                              │
│  ❌ CANNOT run: sudo su, sudo reboot, sudo rm -rf /         │
│  ✅ CAN run: sudo systemctl restart natrium-smm-bot         │
│                                                              │
│  Security principle: Least Privilege                        │
└─────────────────────────────────────────────────────────────┘
```

---

## ⏱️ Timeline деплоя

```
Time  │ GitHub Actions                 │ Oracle VM
──────┼───────────────────────────────┼────────────────────────────
00:00 │ Push to main detected          │
00:02 │ Workflow started               │
00:05 │ Runner provisioned             │
00:07 │ SSH connection established     │ → Receives SSH connection
00:08 │                                │ → cd /opt/natrium-smm-bot
00:10 │                                │ → git pull origin main
00:15 │                                │ → Updating .env
00:17 │                                │ → Installing dependencies
00:45 │                                │ → pip install complete
00:47 │                                │ → systemctl stop bot
00:49 │                                │ → Bot stopped
00:52 │                                │ → systemctl start bot
00:55 │                                │ → Bot starting...
00:58 │                                │ → Bot fully started ✅
01:00 │ Checking status                │ → systemctl status returned
01:02 │ ✅ Deployment successful       │
01:03 │ Workflow completed             │
──────┴───────────────────────────────┴────────────────────────────

Total time: ~1 minute
```

---

## 📈 Улучшения метрик

### Время деплоя
```
Старый способ (ручной):
├─ SSH подключение:        ~10 сек
├─ git pull:               ~5 сек
├─ Редактирование .env:    ~30 сек (вручную)
├─ pip install:            ~30 сек
├─ systemctl restart:      ~5 сек
└─ Проверка:              ~10 сек
   ИТОГО:                  ~90 секунд + человеческий фактор

Новый способ (автоматический):
├─ Workflow старт:         ~5 сек
├─ SSH setup:              ~2 сек
├─ git pull:               ~5 сек
├─ Авто-обновление .env:   ~2 сек
├─ pip install:            ~30 сек
├─ systemctl restart:      ~5 сек
└─ Проверка:              ~3 сек
   ИТОГО:                  ~52 секунды

Выигрыш: ~40 секунд + нет ручного труда
```

### Надежность
```
Старый способ:
- Человеческий фактор: опечатки в .env ❌
- Забыть git pull: возможно ❌
- Забыть pip install: возможно ❌
- Не проверить статус: часто ❌

Новый способ:
- Всегда одинаковая последовательность ✅
- Всегда обновляет .env из Secrets ✅
- Всегда устанавливает зависимости ✅
- Всегда проверяет статус ✅
```

---

**Документ создан для визуализации CI/CD архитектуры проекта natrium-smm-bot**
