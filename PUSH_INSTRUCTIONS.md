# 🚀 Инструкция по отправке кода на GitHub

## 📊 Текущий статус:

✅ **Git репозиторий**: инициализирован  
✅ **Коммиты**: созданы  
✅ **Remote origin**: добавлен (https://github.com/isolovyev77/natrium-smm-bot.git)  
✅ **GitHub Secrets**: настроены  
⏳ **Push на GitHub**: ожидает выполнения  

---

## 🔐 Проблема с аутентификацией

GitHub **НЕ принимает** обычный пароль от аккаунта при `git push`.

### ❌ Не работает:
```
Password: ваш_пароль_от_google
```

### ✅ Работает:
```
Password: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 📝 Шаги для успешного push:

### 1️⃣ Создайте Personal Access Token (PAT)

1. Откройте: **https://github.com/settings/tokens/new**
2. Заполните форму:
   - **Note**: `natrium-smm-bot` (название для себя)
   - **Expiration**: `90 days` (или No expiration)
   - **Scopes**: ✅ **repo** (полный доступ к репозиториям)
3. Нажмите **Generate token**
4. **⚠️ СКОПИРУЙТЕ токен!** (выглядит как `ghp_abcdefgh123456...`)

### 2️⃣ Выполните push с токеном

В терминале запустите:

```bash
cd /Users/isolovyev/Projects/smm_bot/NatriumSMM
git push -u origin main
```

Когда появится запрос:

```
Username for 'https://github.com': isolovyev77
Password for 'https://isolovyev77@github.com': [ВСТАВЬТЕ ТОКЕН СЮДА]
```

**Вставьте скопированный токен вместо пароля** (Cmd+V)

### 3️⃣ Токен сохранится автоматически

После первого ввода macOS сохранит токен в Keychain — больше вводить не придётся!

---

## 🔄 Альтернатива: SSH (рекомендуется для постоянной работы)

### Создайте SSH ключ:

```bash
# Генерация ключа
ssh-keygen -t ed25519 -C "isolovyev77@gmail.com"
# Нажмите Enter 3 раза

# Скопируйте публичный ключ
cat ~/.ssh/id_ed25519.pub
```

### Добавьте на GitHub:

1. Откройте: **https://github.com/settings/keys**
2. **New SSH key** → вставьте содержимое `id_ed25519.pub`

### Измените remote на SSH:

```bash
git remote set-url origin git@github.com:isolovyev77/natrium-smm-bot.git
git push -u origin main
```

---

## ✅ Проверка после успешного push:

Откройте: **https://github.com/isolovyev77/natrium-smm-bot**

Должно отображаться:
- ✅ README.md на главной странице
- ✅ Все файлы проекта (кроме `.env`)
- ✅ Лицензия MIT (справа внизу)
- ✅ 2 коммита в истории

---

## 🆘 Troubleshooting

### Ошибка: "remote: Repository not found"
→ Проверьте название репозитория на GitHub

### Ошибка: "Authentication failed"
→ Используйте Personal Access Token вместо пароля

### Ошибка: "! [rejected]"
→ Выполните `git pull origin main --rebase` и повторите push

---

**Следующий шаг после push:**  
Заявка на **JetBrains Community Support** для получения бесплатной лицензии PyCharm.
