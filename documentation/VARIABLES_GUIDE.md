# 📋 ПЕРЕМЕННЫЕ В YANDEX AGENT API — КАК ЭТО РАБОТАЕТ

## ✅ ИСПРАВЛЕНО: Переменные восстановлены в системном промпте!

---

## 🔑 ТРИ КЛЮЧЕВЫЕ ПЕРЕМЕННЫЕ

```python
variables = {
    "TECHNIQUE": "zero_shot" | "cov+cok" | "few_shot",
    "USER_THEME": "" | "тема №5" | "название темы",
    "POST_LENGTH": "500" | "600" | "700"  # по умолчанию 500, диапазон 200-1000
}
```

### 1. **{{TECHNIQUE}}** — Техника промптинга

**Назначение**: Определяет глубину анализа и стиль генерации.

**Значения**:
- `"zero_shot"` — быстрая генерация без примеров
- `"cov+cok"` — Chain of Verification + Chain of Knowledge (с проверкой фактов) ⭐ рекомендуется
- `"few_shot"` — генерация с примерами из FileSearch

**Где используется**:
- При генерации тем: определяет глубину поиска в FileSearch
- При генерации постов: определяет структуру и проверку фактов

---

### 2. **{{USER_THEME}}** — Тема для поста (или пустая строка)

**Назначение**: **КЛЮЧЕВАЯ ПЕРЕМЕННАЯ** для управления режимом работы агента.

**Значения**:
- `""` (пустая строка) → **РЕЖИМ 1: Генерация тем**
- `"тема №5"` или `"Сон и восстановление"` → **РЕЖИМ 2: Генерация поста**

**Логика в промпте**:
```
ЕСЛИ {{USER_THEME}} == "" (пустая строка):
   → ГЕНЕРИРУЙ 10 ТЕМ
   → СТОП! НЕ ГЕНЕРИРУЙ ПОСТ!

ЕСЛИ {{USER_THEME}} != "" (указана тема):
   → ГЕНЕРИРУЙ ПОСТ НА ЭТУ ТЕМУ
   → НЕ ГЕНЕРИРУЙ НОВЫЕ ТЕМЫ!
```

**Это решает вашу проблему**: агент не будет генерировать пост вместо тем и наоборот!

---

### 3. **{{POST_LENGTH}}** — Длина поста

**Назначение**: Целевая длина поста в символах.

**Значения**:
- `"500"` — стандартный пост (по умолчанию)
- `"600"` или `"700"` — рекомендуемый диапазон 500-700
- Минимум: `"200"`, Максимум: `"1000"`

**Где используется**:
- В режиме генерации поста ({{USER_THEME}} != "")
- Допустимый диапазон отклонения: ±50 символов

---

## 💻 КАК ИСПОЛЬЗОВАТЬ В КОДЕ

### **Вариант 1: Генерация тем** ({{USER_THEME}} = "")

```python
# bot.py
def generate_themes(self, technique: str = "cov+cok") -> str:
    """Генерирует 10 тем"""
    response = self.client.responses.create(
        prompt={
            "id": self.agent_id,
            "variables": {
                "TECHNIQUE": technique,      # zero_shot / cov+cok / few_shot
                "USER_THEME": "",            # ✅ ПУСТАЯ СТРОКА → ГЕНЕРАЦИЯ ТЕМ
                "POST_LENGTH": "1000"
            }
        },
        input="Выполни задачу"
    )
    return response.output_text
```

**Что делает агент**:
1. Видит `{{USER_THEME}} == ""`
2. Переходит в **РЕЖИМ 1: Генерация тем**
3. Использует технику `{{TECHNIQUE}}` для поиска в FileSearch + Web Search
4. Генерирует 10 тем
5. **СТОП! НЕ генерирует пост!**

---

### **Вариант 2: Генерация поста** ({{USER_THEME}} != "")

```python
# bot.py
def generate_post(self, theme: str, technique: str = "cov+cok", post_length: int = 1000) -> str:
    """Генерирует пост по теме"""
    response = self.client.responses.create(
        prompt={
            "id": self.agent_id,
            "variables": {
                "TECHNIQUE": technique,          # zero_shot / cov+cok / few_shot
                "USER_THEME": theme,             # ✅ "тема №5" → ГЕНЕРАЦИЯ ПОСТА
                "POST_LENGTH": str(post_length)
            }
        },
        input="Выполни задачу"
    )
    return response.output_text
```

**Что делает агент**:
1. Видит `{{USER_THEME}} != ""`
2. Переходит в **РЕЖИМ 2: Генерация поста**
3. Генерирует пост на тему `{{USER_THEME}}`
4. Использует технику `{{TECHNIQUE}}` (zero_shot / cov+cok / few_shot)
5. Длина поста: `{{POST_LENGTH}}` символов
6. **НЕ генерирует новые темы!**

---

## 🎯 ПРИМЕР ПОЛНОГО ЦИКЛА

```python
# main.py

def main():
    bot = NatriumBot()
    
    # 1. Запрос техники
    print("Выберите технику:")
    print("1️⃣ zero_shot")
    print("2️⃣ cov+cok (рекомендуется)")
    print("3️⃣ few_shot")
    
    choice = input("\nВведите номер (1-3): ").strip()
    technique_map = {"1": "zero_shot", "2": "cov+cok", "3": "few_shot"}
    technique = technique_map.get(choice, "cov+cok")
    
    # 2. ГЕНЕРАЦИЯ ТЕМ (USER_THEME = "")
    print(f"\n🔄 Генерация тем с техникой {technique}...\n")
    
    response = bot.client.responses.create(
        prompt={
            "id": bot.agent_id,
            "variables": {
                "TECHNIQUE": technique,
                "USER_THEME": "",           # ✅ ПУСТАЯ СТРОКА → ГЕНЕРАЦИЯ ТЕМ
                "POST_LENGTH": "1000"
            }
        },
        input="Выполни задачу"
    )
    
    themes = response.output_text
    print(themes)
    
    # 3. Выбор темы
    theme_choice = input("\nВыберите тему (1-10) или напишите свою: ").strip()
    
    # 4. ГЕНЕРАЦИЯ ПОСТА (USER_THEME = "тема №5")
    print(f"\n✍️ Генерация поста с техникой {technique}...\n")
    
    response = bot.client.responses.create(
        prompt={
            "id": bot.agent_id,
            "variables": {
                "TECHNIQUE": technique,
                "USER_THEME": theme_choice,  # ✅ УКАЗАНА ТЕМА → ГЕНЕРАЦИЯ ПОСТА
                "POST_LENGTH": "1000"
            }
        },
        input="Выполни задачу"
    )
    
    post = response.output_text
    print(post)
    
    # 5. Сохранение
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    with open(f"output/posts/post_{timestamp}.md", "w", encoding="utf-8") as f:
        f.write(post)
    
    print(f"\n✅ Пост сохранён: output/posts/post_{timestamp}.md")
```

---

## 🔍 КАК АГЕНТ ЧИТАЕТ ПЕРЕМЕННЫЕ

В системном промпте агента переменные заменяются **автоматически**:

**Запрос 1: Генерация тем**
```python
variables = {
    "TECHNIQUE": "cov+cok",
    "USER_THEME": "",        # ← агент видит ПУСТУЮ СТРОКУ
    "POST_LENGTH": "1000"
}
```

**Агент читает промпт**:
```
ПРОВЕРКА: {{USER_THEME}} == "" ?

ЕСЛИ {{USER_THEME}} == "" (пустая строка):  ← ✅ ДА, это наш случай!
   → ГЕНЕРИРУЙ 10 ТЕМ
```

---

**Запрос 2: Генерация поста**
```python
variables = {
    "TECHNIQUE": "cov+cok",
    "USER_THEME": "Сон и восстановление",  # ← агент видит ТЕМУ
    "POST_LENGTH": "1000"
}
```

**Агент читает промпт**:
```
ПРОВЕРКА: {{USER_THEME}} == "" ?

ЕСЛИ {{USER_THEME}} != "" (указана тема):  ← ✅ ДА, это наш случай!
   → ГЕНЕРИРУЙ ПОСТ НА ЭТУ ТЕМУ

ТЕМА: Сон и восстановление
ДЛИНА: 1000 символов
ТЕХНИКА: cov+cok
```

---

## ⚡ КЛЮЧЕВЫЕ ПРАВИЛА

1. **{{USER_THEME}} == ""** → генерируй ТОЛЬКО темы, НЕ генерируй пост
2. **{{USER_THEME}} != ""** → генерируй ТОЛЬКО пост, НЕ генерируй новые темы
3. **{{TECHNIQUE}}** → определяет глубину анализа и стиль генерации
4. **{{POST_LENGTH}}** → целевая длина поста (±100 символов)
5. **Темы НЕ БОЛЕЕ 5 СЛОВ каждая**
6. **Источники в квадратных скобках [WHO], [CrossFit], [Богачев]**

---

## 🎉 ИТОГ

✅ **Переменные восстановлены** в системном промпте  
✅ **{{USER_THEME}} == ""** → генерация тем  
✅ **{{USER_THEME}} != ""** → генерация поста  
✅ **Проблема с генерацией новых тем вместо поста решена!**

---

**Дата**: 25 января 2026  
**Статус**: ✅ Готово к использованию
