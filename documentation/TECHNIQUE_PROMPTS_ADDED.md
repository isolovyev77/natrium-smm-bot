# ✅ ДОБАВЛЕНЫ ИНСТРУКЦИИ ТЕХНИК В ПОЛЬЗОВАТЕЛЬСКИЙ ПРОМПТ

## 🔍 АНАЛИЗ ФАЙЛОВ В ПАПКЕ PROMPTS

### **Что есть в папке `prompts/`**:

**Используются**:
- ✅ `agent_system_prompt.md` — **загружен в Yandex Agent** (основной системный промпт)

**НЕ используются (но есть)**:
- ❌ `zero_shot_post.md` — инструкции для техники zero-shot
- ❌ `cov_cok_post.md` — инструкции для техники CoV+CoK
- ❌ `few_shot_post.md` — инструкции для техники Few-Shot

**Документация**:
- 📋 `README.md`, `STYLE_GUIDE_FINAL.md`, `VARIABLES_GUIDE.md` и др.

---

## 💡 ПРОБЛЕМА

**Файлы с инструкциями для техник существуют, но НЕ используются!**

### **Что было в коде**:
```python
def generate_post(self, theme, technique, post_length):
    # Только короткая базовая инструкция
    input_text = f"Сгенерируй пост на тему {theme} с использованием техники {technique}"
    return self._call_api(variables, input_text)
```

**Проблема**:
- Агент не знает **как именно** работать с каждой техникой
- CoV+CoK должен проверять источники — но где это написано?
- Few-Shot должен использовать примеры — но агент об этом не знает!

---

## ✅ РЕШЕНИЕ: ГИБРИДНЫЙ ПОДХОД

### **Не загружаем весь файл** (слишком длинно, 40-75 строк)

### **Добавляем ключевые инструкции** для каждой техники

**НОВЫЙ КОД**:

```python
def generate_post(self, theme, technique, post_length):
    # Базовая инструкция (краткая)
    input_text = f"Сгенерируй пост на тему {theme} с использованием техники {technique}"
    
    # Добавляем ключевые инструкции для каждой техники
    if technique == "cov+cok":
        input_text += """\n\nВАЖНО для CoV+CoK:
- Используй FileSearch (материалы Богачева, CrossFit) и Web Search для проверки фактов
- Добавляй источники в квадратных скобках: [WHO], [CrossFit], [PubMed], [Исследования]
- Проверяй актуальность данных (2025-2026)
- Каждый факт должен иметь подтверждение"""
    
    elif technique == "few_shot":
        input_text += """\n\nВАЖНО для Few-Shot:
- Изучи примеры успешных постов в FileSearch
- Используй их структуру, стиль и ритм
- Сохрани тон и энергетику Натриум Фитнесс
- Повтори паттерны лучших постов"""
    
    # zero_shot — без дополнений (быстрая генерация)
    
    return self._call_api(variables, input_text)
```

---

## 📊 СРАВНЕНИЕ ПОДХОДОВ

### **Подход 1: Загрузка полного файла** ❌
```python
prompt_file = self.prompts_dir / f"{technique}_post.md"
with open(prompt_file) as f:
    technique_prompt = f.read()  # 40-75 строк!

input_text = f"{technique_prompt}\n\nТема: {theme}"
```

**Проблемы**:
- 40-75 строк в каждом запросе
- Увеличение токенов и стоимости
- Может конфликтовать с agent_system_prompt
- Медленнее обработка

### **Подход 2: Только базовая инструкция** ⚠️
```python
input_text = f"Сгенерируй пост на тему {theme} с использованием техники {technique}"
```

**Проблемы**:
- Агент не знает как работать с техникой
- CoV+CoK не проверяет источники
- Few-Shot не использует примеры

### **Подход 3: Ключевые инструкции** ✅ (ВЫБРАЛИ)
```python
input_text = f"Сгенерируй пост на тему {theme} с использованием техники {technique}"

if technique == "cov+cok":
    input_text += "\n\nВАЖНО для CoV+CoK:\n- проверка фактов\n- источники..."
```

**Преимущества**:
- ✅ Короткие ключевые инструкции (4-5 строк вместо 40-75)
- ✅ Агент знает специфику техники
- ✅ Не перегружает запрос
- ✅ Лучший баланс краткости и детализации

---

## 🎯 ЧТО ПЕРЕДАЁТСЯ АГЕНТУ

### **Техника: zero_shot**
```
Input: "Сгенерируй пост на тему Развитие кардиофитнеса с использованием техники zero_shot"

(без дополнений — быстрая генерация на общих знаниях)
```

### **Техника: cov+cok**
```
Input: "Сгенерируй пост на тему Развитие кардиофитнеса с использованием техники cov+cok

ВАЖНО для CoV+CoK:
- Используй FileSearch (материалы Богачева, CrossFit) и Web Search для проверки фактов
- Добавляй источники в квадратных скобках: [WHO], [CrossFit], [PubMed], [Исследования]
- Проверяй актуальность данных (2025-2026)
- Каждый факт должен иметь подтверждение"
```

### **Техника: few_shot**
```
Input: "Сгенерируй пост на тему Развитие кардиофитнеса с использованием техники few_shot

ВАЖНО для Few-Shot:
- Изучи примеры успешных постов в FileSearch
- Используй их структуру, стиль и ритм
- Сохрани тон и энергетику Натриум Фитнесс
- Повтори паттерны лучших постов"
```

---

## 📋 ОТКУДА ВЗЯТЫ ИНСТРУКЦИИ

### **Из файла `cov_cok_post.md`** (упрощено):
**БЫЛО** (40+ строк):
```markdown
**ШАГ 1: ИЗВЛЕЧЕНИЕ ЗНАНИЙ**
1. FileSearch: Найди информацию...
   - Богачев: периодизация...
   - CrossFit L1/L2...
2. Web Search: Проверь актуальные данные...

**ШАГ 2: ВЕРИФИКАЦИЯ ФАКТОВ**
✅ Проверь каждый факт...
❌ НЕ ИСПОЛЬЗУЙ...

**ШАГ 3: ГЕНЕРАЦИЯ ПОСТА**
[структура...]
```

**СТАЛО** (4 строки):
```
- Используй FileSearch и Web Search для проверки фактов
- Добавляй источники в квадратных скобках
- Проверяй актуальность данных (2025-2026)
- Каждый факт должен иметь подтверждение
```

### **Из файла `few_shot_post.md`** (упрощено):
**БЫЛО** (50+ строк с примерами):
```markdown
**ШАГ 1: ИЗУЧИ ПРИМЕРЫ**
Посмотри на примеры успешных постов...

**ШАГ 2: ИЗВЛЕКИ ПАТТЕРНЫ**
- Какие заголовки...
- Как структурированы...

**ШАГ 3: ГЕНЕРАЦИЯ ПО ПАТТЕРНУ**
[примеры структур...]
```

**СТАЛО** (4 строки):
```
- Изучи примеры успешных постов в FileSearch
- Используй их структуру, стиль и ритм
- Сохрани тон и энергетику Натриум Фитнесс
- Повтори паттерны лучших постов
```

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

### **Zero-Shot**:
- Быстрая генерация
- На основе общих знаний
- Без проверки источников
- Хорошо для простых тем

### **CoV+CoK**:
- ✅ Проверка фактов в FileSearch и Web Search
- ✅ Источники в квадратных скобках: `[WHO]`, `[PubMed]`
- ✅ Актуальные данные 2025-2026
- ✅ Более надёжный контент

### **Few-Shot**:
- ✅ Изучение примеров успешных постов
- ✅ Повторение их структуры и стиля
- ✅ Сохранение тона Натриум Фитнесс
- ✅ Более качественный текст

---

## 📝 ОБНОВЛЁННЫЕ ФАЙЛЫ

**Файл**: `src/bot.py`

**Метод**: `generate_post()`

**Что изменилось**:
- ✅ Добавлены условия для `cov+cok` и `few_shot`
- ✅ Каждая техника получает свои ключевые инструкции
- ✅ Zero-shot остаётся без дополнений (быстрая генерация)

---

## 🚀 СТАТУС ФАЙЛОВ В PROMPTS

### **Используются**:
1. ✅ `agent_system_prompt.md` — загружен в Yandex Agent (системный промпт)
2. ✅ Ключевые инструкции из `cov_cok_post.md` — **теперь в коде**
3. ✅ Ключевые инструкции из `few_shot_post.md` — **теперь в коде**

### **Не используются** (но можно использовать для справки):
- 📋 `zero_shot_post.md`, `cov_cok_post.md`, `few_shot_post.md` — полные версии
- 📋 `STYLE_GUIDE_FINAL.md`, `VARIABLES_GUIDE.md` — документация

### **Можно удалить** (дублируют информацию):
- ❌ `chain_of_knowledge.md`, `cov_cok_post.md` (старые версии)
- ❌ `test_post_prompt.md`, `test_themes_prompt.md` (тестовые)

---

## ✅ ИТОГ

**Статус**: ✅ **ОПТИМИЗИРОВАНО!**

**Что сделано**:
1. ✅ Проанализированы файлы в `prompts/`
2. ✅ Извлечены ключевые инструкции для каждой техники
3. ✅ Добавлены в код (4-5 строк вместо 40-75)
4. ✅ Баланс между краткостью и детализацией

**Ожидаемый эффект**:
- Техника CoV+CoK теперь **реально проверяет факты** и **добавляет источники**
- Техника Few-Shot **изучает примеры** и **повторяет их стиль**
- Zero-Shot остаётся **быстрым** (без дополнительных инструкций)

---

**Дата**: 25 января 2026  
**Статус**: ✅ Инструкции техник интегрированы в код!
