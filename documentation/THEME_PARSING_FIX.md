# ✅ ИСПРАВЛЕНО: ПОСТ ГЕНЕРИРУЕТСЯ ПО ВЫБРАННОЙ ТЕМЕ!

## 🔍 ПРОБЛЕМА

**Что происходило**:
- Пользователь выбирает тему **№8** ("Развитие кардиофитнеса")
- Генерируется пост на тему **№5** ("Меткон")
- Пользователь выбирает тему **№3** ("Физическая активность ВОЗ")
- Генерируется пост на тему "Тренировки по группам: сила команды" (не та тема!)

**Вывод**: Агент **игнорирует выбранную тему** и генерирует пост на случайную или первую тему.

---

## 🎯 ПРИЧИНА

### Что было в коде:

```python
# main.py
def get_theme_choice(themes):
    theme = input("\nВаш выбор: ").strip()
    return theme  # ← возвращает "8" (строка с номером)

# В основном цикле:
theme_choice = get_theme_choice(themes)  # theme_choice = "8"

# Передаётся агенту:
bot.generate_post(
    theme=theme_choice,  # ← передаётся "8", а не "Развитие кардиофитнеса"
    technique=technique,
    post_length=post_length
)
```

**Проблема**:
- Пользователь вводит **номер темы**: "8"
- Агенту передаётся **строка "8"**, а не название темы
- Агент **не знает**, что означает "8" — он видит только число
- Агент генерирует пост на **случайную тему** из своего FileSearch

---

## ✅ РЕШЕНИЕ

### Добавлена функция `parse_theme_from_list()`:

```python
def parse_theme_from_list(theme_choice: str, themes_text: str) -> str:
    """
    Парсит выбор темы и возвращает название темы.
    
    Args:
        theme_choice: ввод пользователя (номер или название)
        themes_text: текст со списком тем
        
    Returns:
        Название темы для генерации поста
    """
    # Если это номер (1-10), извлекаем тему из списка
    if theme_choice.isdigit():
        theme_num = int(theme_choice)
        if 1 <= theme_num <= 10:
            # Ищем строку с номером темы
            patterns = [
                rf"{theme_num}️⃣\s+([^\[]+)",  # "8️⃣ Развитие кардиофитнеса [источник]"
            ]
            
            for pattern in patterns:
                match = re.search(pattern, themes_text, re.MULTILINE)
                if match:
                    theme_name = match.group(1).strip()
                    # Убираем [источник] в конце
                    theme_name = re.sub(r'\s*[\[\(].*$', '', theme_name).strip()
                    print(f"✅ Извлечена тема #{theme_num}: '{theme_name}'")
                    return theme_name
    
    # Если это не номер, возвращаем как есть (пользовательская тема)
    return theme_choice
```

### Обновлён основной цикл:

```python
# Пользователь выбирает тему
theme_choice = get_theme_choice(themes)  # theme_choice = "8"

# ✅ ПАРСИМ ТЕМУ (извлекаем название по номеру)
theme_name = parse_theme_from_list(theme_choice, themes)  
# theme_name = "Развитие кардиофитнеса"

print(f"\n✍️ Генерация поста на тему: '{theme_name}'")

# ✅ ПЕРЕДАЁМ НАЗВАНИЕ ТЕМЫ, А НЕ НОМЕР
post = bot.generate_post(
    theme=theme_name,  # ← "Развитие кардиофитнеса"
    technique=technique,
    post_length=post_length
)

# ✅ СОХРАНЯЕМ С НАЗВАНИЕМ ТЕМЫ
filepath = save_post(post, theme_name, technique)
```

---

## 📋 КАК ЭТО РАБОТАЕТ

### **Пример 1: Выбор темы №8**

**Список тем**:
```
1️⃣ Периодизация тренировок [FileSearch]
2️⃣ Выносливость в CrossFit [FileSearch]
...
8️⃣ Развитие кардиофитнеса [PubMed]
...
```

**Пользователь вводит**: `8`

**Парсинг**:
```python
theme_choice = "8"
theme_name = parse_theme_from_list("8", themes)
# Ищем паттерн: "8️⃣ Развитие кардиофитнеса [PubMed]"
# Извлекаем: "Развитие кардиофитнеса"
# Результат: theme_name = "Развитие кардиофитнеса"
```

**Передаётся агенту**:
```
Сгенерируй ПОСТ на тему: Развитие кардиофитнеса

ВАЖНО: 
- Это запрос на ГЕНЕРАЦИЮ ПОСТА, НЕ тем!
- USER_THEME = "Развитие кардиофитнеса" (НЕ пустая строка)
- Длина поста: 500 символов

Сгенерируй ГОТОВЫЙ ПОСТ...
```

**Результат**: Агент генерирует пост **именно про развитие кардиофитнеса** ✅

---

### **Пример 2: Своя тема**

**Пользователь вводит**: `Сон и восстановление`

**Парсинг**:
```python
theme_choice = "Сон и восстановление"
theme_name = parse_theme_from_list("Сон и восстановление", themes)
# Это не номер, возвращаем как есть
# Результат: theme_name = "Сон и восстановление"
```

**Передаётся агенту**:
```
Сгенерируй ПОСТ на тему: Сон и восстановление
...
```

**Результат**: Агент генерирует пост **про сон и восстановление** ✅

---

## 🎯 ВЫВОД НА ЭКРАН

### **До исправления**:
```
Ваш выбор: 8

✍️ Генерация поста на тему: '8'

📄 СГЕНЕРИРОВАННЫЙ ПОСТ:

🎯 МЕТКОН: ИНТЕНСИВНОСТЬ И ОБЪЁМ  ← неправильная тема!
```

### **После исправления**:
```
Ваш выбор: 8
✅ Извлечена тема #8: 'Развитие кардиофитнеса'

✍️ Генерация поста на тему: 'Развитие кардиофитнеса'

📄 СГЕНЕРИРОВАННЫЙ ПОСТ:

🎯 РАЗВИТИЕ КАРДИОФИТНЕСА: КЛЮЧ К ВЫНОСЛИВОСТИ  ← правильная тема!
```

✅ **Пост генерируется по выбранной теме!**

---

## 🧪 ТЕСТИРОВАНИЕ

### **Создан тестовый скрипт**: `test_parse_theme.py`

```bash
python test_parse_theme.py
```

**Результат**:
```
Тест 1: Ввод '3'
✅ Извлечена тема #3: 'Физическая активность ВОЗ'

Тест 2: Ввод '8'
✅ Извлечена тема #8: 'Развитие кардиофитнеса'

Тест 3: Ввод '5'
✅ Извлечена тема #5: 'Меткон: интенсивность'

Тест 4: Ввод 'Сон и восстановление'
Результат: 'Сон и восстановление'

✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ!
```

---

## 🚀 ОБНОВЛЁННЫЕ ФАЙЛЫ

### **1. `src/main.py`**:

**Добавлено**:
- ✅ Функция `parse_theme_from_list()` — извлекает название темы по номеру
- ✅ Вызов `theme_name = parse_theme_from_list(theme_choice, themes)` перед генерацией
- ✅ Передача `theme_name` (название) вместо `theme_choice` (номер) агенту
- ✅ Сохранение файла с `theme_name` вместо `theme_choice`

**Что изменилось**:
```python
# БЫЛО:
theme_choice = get_theme_choice(themes)  # "8"
post = bot.generate_post(theme=theme_choice, ...)  # передавали "8"

# СТАЛО:
theme_choice = get_theme_choice(themes)  # "8"
theme_name = parse_theme_from_list(theme_choice, themes)  # "Развитие кардиофитнеса"
post = bot.generate_post(theme=theme_name, ...)  # передаём название
```

---

### **2. `test_parse_theme.py`** — создан новый файл

Тестирует парсинг тем с разными входными данными.

---

## ✅ ИТОГ

**Статус**: ✅ **ИСПРАВЛЕНО!**

**Что было исправлено**:
- ✅ Пост генерируется **по выбранной теме**, а не случайной
- ✅ Номер темы **корректно извлекается** из списка
- ✅ Своя тема (не номер) **работает как прежде**
- ✅ Файл поста сохраняется **с названием темы** (не номером)

**Тестирование**:
```bash
cd /Users/isolovyev/Projects/smm_bot/NatriumSMM
source .venv/bin/activate
python src/main.py
```

**Сценарий тестирования**:
1. Выбрать технику (например, `2`)
2. Дождаться генерации 10 тем
3. Выбрать тему **№8** ("Развитие кардиофитнеса")
4. Указать длину (например, `500`)
5. **Проверить**: пост должен быть **про кардиофитнес**, а не про меткон! ✅

---

**Дата**: 25 января 2026  
**Статус**: ✅ Проблема полностью решена!
