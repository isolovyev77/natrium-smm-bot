# Передача дневника питания Natrium

## Выпуск UX-поправок 8 сентября 2026

Узкие поправки поверх production commit
`23f0b6a8f0e17955924e8119861562cf410ee103` опубликованы commit
`1ca93b850a91271289c1061ef229f95792411150`. GitHub Actions run `34178310051`
(`Deploy to Oracle Cloud #69`) завершился со статусом `success`.

- конфликт версии сохраняет введенные поля и позволяет принять серверную
  версию либо явно повторить изменение поверх нее;
- клиент может заново получить свою запись еды, воды или веса по ID, в том
  числе после переноса даты или мягкой отмены; чужой и отсутствующий объект
  одинаково возвращают `404`;
- подтверждение приема проверяет `If-Match`: измененный после предпросмотра
  черновик возвращает `409`, а повтор уже успешного запроса с тем же
  `Idempotency-Key` остается безопасным;
- неопределенный сетевой результат позволяет восстановить отправленные поля
  и повторить тот же payload с прежним ключом. Изменение данных блокируется до
  разрешения предыдущего запроса; следующая самостоятельная операция получает
  новый ключ;
- несуществующее местное время при переводе часов отклоняется до записи, пустые
  обязательные поля ручного приема не превращаются в нули и не отправляются;
- справка распознает вопросы о доступных функциях и текущем поле, сохраняя
  незавершенный Telegram-визард.

Перед выпуском создан приватный backup
`/opt/natrium-smm-bot-backups/nutrition-ux-20260908T013757Z-453c62` текущего
runtime `23f0b6a8f0e17955924e8119861562cf410ee103` и schema 4.

Финальный общий прогон на изолированной Mac Studio: `225 passed` в
`tests/test_nutrition*.py` за `11.71s`, с Node.js в `PATH`. Отдельные проверки
на синтетическом API подтвердили восстановление после конфликтов всех
версионируемых форм, свежую версию перед подтверждением, безопасный повтор
после неопределенного сетевого ответа, DST-валидацию и отсутствие запросов при
пустом ручном вводе. Неожиданных HTTP- и browser-ошибок не было.

Живая проверка подтвердила exact SHA и совпадение четырех измененных модулей с
принятыми исходниками. Сервис активен, `NRestarts=0`; веб-сервер и планировщик
запущены без новых ошибок приложения. `getMe` подтвердил `natrium_smm_bot`.
Schema 4 не менялась: все 21 таблица совпали с резервной копией по числу строк
и контрольным суммам, `integrity_check=ok`, нарушений внешних ключей нет.
Публичная HTTPS-страница отвечает `200` и совпадает с исходным HTML после
нормализации CSP nonce; шесть защищенных API, включая новые чтения отдельных
записей, отвечают `401` без авторизации. Проверка не создавала пользовательские
данные и не отправляла реальные сообщения.

## Выпуск кабинетов и напоминаний 8 сентября 2026

Расширение поверх production commit
`96c9d8389a0bc159fa0222a27dda0edc4a3773f2` опубликовано commit
`23f0b6a8f0e17955924e8119861562cf410ee103`. GitHub Actions run
`34175431073` (`Deploy to Oracle Cloud #68`) завершился со статусом `success`.

Состав выпуска:

- клиентский и тренерский веб-кабинеты с серверной проверкой Telegram
  `initData`, одноразовым входом для обычного браузера, короткой cookie-сессией,
  CSRF, текущей проверкой роли и связи на каждом запросе;
- профиль клиента, вес, контекст приема пищи, исправление и мягкая отмена своих
  подтвержденных записей, воды и веса с версией и аудитом;
- день, неделя и период до 366 дней с раздельным покрытием питания, воды и веса,
  `null` для отсутствующей записи, исторической нормой на каждый день и
  корректным часовым поясом клиента;
- безопасные CSV, XLSX и HTML для печати с нормами, водой, весом и локальным
  временем, защитой от формул и экранированием HTML;
- настройки напоминаний клиента и ежедневной сводки тренера. Все выключено по
  умолчанию. Планировщик запускается только при
  `NUTRITION_REMINDERS_ENABLED=1`, берет свежие слоты, устойчиво исключает
  дубли и заново проверяет настройки, allowlist и связи перед отправкой;
- Telegram-визарды профиля, веса, контекста приема, напоминаний, входа в
  веб-кабинет и контекстная справка по текущему шагу;
- повтор мобильного POST с тем же `Idempotency-Key` не дублирует черновик или
  комментарий, а тот же ключ с другими данными отвечает конфликтом.

Схема 4 сохраняет schema 2 и добавляет профили, журнал веса, безопасные
веб-сессии, XLSX preview, настройки напоминаний, очередь отправки и служебную
дедупликацию. Миграции выполняются в явных транзакциях. Финальный полный прогон
на изолированной Mac Studio: `214 passed` в `tests/test_nutrition*.py` за
`9.89s`. Проверены HTTP auth/CSRF/Origin, права после отвязки, версии и
идемпотентность, справочный provenance, экспорт, миграции, отсутствие старой
догоняющей рассылки, обход более 500 opt-in пользователей без starvation и
асинхронную отправку через fake sender. Отдельный
синтетический preview использует 2 тренеров и 2 клиентов с разными часовыми
поясами и не содержит production-данных.

UI-приемка на настоящем синтетическом API завершена без неожиданных HTTP- и
browser-ошибок. На согласованной копии живой SQLite проверен переход schema
2 -> 4: `integrity_check=ok`, нарушений внешних ключей нет, counts и хеши
проекций всех прежних строк совпали. До изменения runtime создан приватный
backup `/opt/natrium-smm-bot-backups/nutrition-v3-20260908T005712Z-fa0bd9`
с кодом, конфигурацией, systemd, Tailscale serve и согласованной SQLite.

После выпуска production находится на exact commit
`23f0b6a8f0e17955924e8119861562cf410ee103`. Сервис `natrium-smm-bot` активен,
`NRestarts=0`, schema 4, прежние 10 таблиц совпали с baseline. Dashboard и
планировщик запущены с явными флагами, ошибок запуска нет. HTTPS Funnel
публикует только loopback listener `127.0.0.1:8080`: корневая страница отвечает
`200`, а защищенные API без авторизации отвечают `401`. Настройки напоминаний
пользователей остались выключены, очередь пуста. Никакие реальные сообщения и
тестовые записи пользователей для проверки не создавались.

Откат после перехода на schema 4 ограничен: код schema 2 не читает schema 4.
При ошибке сначала сохраняют текущую базу и исправляют или возвращают
совместимый код. Старую резервную копию нельзя восстанавливать поверх новых
записей, появившихся после миграции.

Известные границы: PDF создается через печать безопасной HTML-версии в браузере,
сервер не формирует отдельный PDF-файл; нормы задает тренер, медицинские
рекомендации автоматически не рассчитываются; ссылка на чат доступна только
при актуальном публичном username. Очередь напоминаний использует at-most-once:
для каждого локального слота выполняется не более одной попытки отправки. Это
исключает автоматический дубль, но временная ошибка Telegram не вызывает
повторную отправку того же уведомления.

## Выпуск v2 8 сентября 2026

Подготовлен один связный релиз поверх production commit `32dea40266b9c155d09b5ca80ae8970546d815ff`:

- SQLite schema v2 с транзакционной миграцией заполненной schema v1;
- локальный offline-справочник USDA на 8105 полных записей, без API-ключа, с сохранением `fdc_id`, версии источника и снимка КБЖУ на 100 г;
- выбор нескольких справочных продуктов и пересчёт по подтверждённой массе;
- честное разделение методов `reference`, `ai`, `manual` и `legacy` без присвоения USDA-происхождения AI-оценкам;
- постоянный код тренера до явной замены с сохранением старых одноразовых кодов и существующих связей;
- комментарий после выбора клиента и приёма по имени, дате и времени, без ввода технического ID;
- пошаговые дневные нормы с единицами и предварительным подтверждением;
- шаблон и атомарный импорт XLSX до 2 МБ и 366 строк;
- прерывание незавершённого ввода через `/start`, `/help`, кнопку дневника и явную отмену.

Локальная приёмка: `143 passed` в `tests/test_nutrition_*.py`; изменённые Python-модули компилируются, `git diff --check` не нашёл ошибок. Проверены полный снимок заполненной v1 базы до и после миграции, `PRAGMA integrity_check`, внешние ключи, искусственный сбой миграции и повторное открытие, конкурентные подключения, устаревшие callback между клиентами, отсутствие частичной записи XLSX и сохранение справочного снимка при замене каталога.

Перед production migration нужно создать новый каталог резервной копии с правами `700`, сохранить код и конфигурацию с правами `600`, а действующую SQLite скопировать через SQLite backup API. В доказательство достаточно сохранить counts по таблицам, версию схемы, `integrity_check` и контрольные агрегаты без имён, содержимого рациона, комментариев и других персональных данных.

Preflight backup создан до публикации: `/opt/natrium-smm-bot-backups/nutrition-v2-20260907T214143Z`. Каталог имеет права `700`, файлы `600`. В нём сохранены commit `32dea40266b9c155d09b5ca80ae8970546d815ff`, архив кода, конфигурация, systemd unit, согласованная SQLite через backup API, SHA-256 файлов и baseline schema v1. Проверка копии: `integrity_check=ok`, нарушений внешних ключей нет; counts по пользовательским таблицам сохранены без содержимого строк.

Откат кода после перехода базы на schema v2 ограничен: прежний код schema v1 не читает schema v2. При неудачном запуске сначала возвращают новый код и исправляют запуск без удаления SQLite. Возврат к старому коду возможен только вместе с отдельной согласованной копией базы v1. Нельзя восстанавливать старую копию поверх production базы, если после миграции появились новые пользовательские записи: сначала нужно остановить сервис, сохранить текущую schema v2 отдельно и принять отдельное решение о переносе данных.

После штатной доставки проверяются фактический commit, active-состояние systemd без цикла перезапусков, schema version 2, целостность и неизменность исходных counts, наличие каталога и XLSX-шаблона, регистрация `/help` и document handler, `getMe`, trainer flag и безопасная выборка критических ошибок. Реальные сообщения, приглашения, комментарии и записи пользователей для smoke-проверки не создаются.

Живой результат основного изменения: commit `4cd75963f8547312101c99ce41b19c6c59ce71c2` опубликован в `main`; GitHub Actions run `34164188325` (`Deploy to Oracle Cloud #65`) завершился со статусом `success` за 37 секунд. Следом commit `dd9ba57e53627ae4e1949bc67c7288ee7e607214` исправил подпись постоянного кода; run `34164785277` (`#66`) завершился со статусом `success` за 29 секунд. После каждого запуска production checkout находился на exact SHA, worktree был чистым. `natrium-smm-bot` имел состояния `active/running`, `NRestarts=0`, PID был стабилен между проверками; в безопасной выборке после выпуска не было совпадений `traceback`, `critical` и `unhandled exception`.

Финальная copy-правка согласия на фото опубликована commit `96c9d8389a0bc159fa0222a27dda0edc4a3773f2`; GitHub Actions run `34165171030` завершился со статусом `success`. Production находится на exact SHA, worktree чистый, сервис `active/running`, `NRestarts=0`, PID стабилен. Schema v2, `integrity_check=ok`, внешние ключи и counts относительно последней копии не изменились; `getMe` и обе новые формулировки подтверждены без отправки сообщений пользователям.

Production SQLite перешла на schema v2: `integrity_check=ok`, нарушений внешних ключей нет. Counts всех 11 исходных таблиц совпали с preflight baseline. По первичным ключам сопоставлены 18 существовавших строк без раскрытия содержимого, все исходные значения сохранились. Ранее погашенный одноразовый код остался `one_time` и не стал активным.

В production подтверждены 8105 справочных записей и один read-only расчёт на 100 г, импорт `openpyxl 3.1.5`, создание XLSX-шаблона, trainer flag владельца, `getMe` для `natrium_smm_bot`, стартовая кнопка, команды `/start`, `/help`, `/nutrition`, `/today`, `/week`, document handler и новые nutrition routes. Веб-панель осталась выключена. Тестовые пользовательские записи, приглашения, комментарии и сообщения не создавались.

Ручная проверка вторым Telegram-аккаунтом остаётся нужна для визуального прохода: подключение постоянным кодом, справочный приём с несколькими продуктами, комментарий тренера, Back/Отмена, мастер норм и реальная загрузка XLSX через Bot API.

## Граница пакета

Пакет подготовлен для применения к чистому checkout репозитория на базовом коммите `d13371a246d0dc223cafe736f10541e4bec9a6c2`. Он содержит только новые файлы дневника питания и три изменённых существующих файла: `src/telegram_bot.py`, `.env.example`, `.gitignore`.

Локальная автоматическая приёмка завершена: `64 passed, 0 failed, 0 skipped` за `0.58s`. Она использовала временную SQLite, синтетические Telegram updates, фиктивные ответы AI и поддельные исходящие вызовы.

Не проверены настоящий Telegram Bot API, OpenAI API, HTTP и Telegram Mini App, живой сервер Oracle и production deployment. Пакет нельзя считать выпущенным до прохождения этих проверок в целевой среде.

## Подготовка выпуска

1. Создать чистый checkout на коммите `d13371a246d0dc223cafe736f10541e4bec9a6c2`. Если удалённая ветка уже ушла вперёд, сначала перенести изменения на её актуальную вершину и повторить проверки.
2. На живом сервере сохранить текущую версию изменяемых файлов, действующий commit SHA и конфигурацию. Секреты не переносить в пакет и не выводить в журнал.
3. Сделать согласованную резервную копию рабочей SQLite через SQLite backup API. Полученный файл самодостаточен: отдельно копировать к нему WAL/SHM работающей базы не нужно. Не удалять и не заменять живую базу при применении кода.
4. До перезапуска вызвать Telegram `getMe` с действующим токеном и проверить точное значение `username == "natrium_smm_bot"`. Токен и полный URL запроса не журналировать.
5. Распаковать архив поверх чистого checkout с сохранением относительных путей. Проверить SHA-256 каждого файла по `manifest.json`.
6. Настроить переменные из `.env.example`. Внешнюю панель оставлять выключенной, пока не готовы HTTPS origin и allowlist: `NUTRITION_DASHBOARD_ENABLED=0`.
7. Выполнить локальные проверки в окружении проекта:

```bash
python3 -m py_compile src/telegram_bot.py src/nutrition_store.py src/nutrition_ai.py src/nutrition_bot.py src/nutrition_dashboard.py
python3 -m pytest tests/test_nutrition_*.py -q
```

8. Выполнить штатный deployment проекта без изменения его схемы доставки. После запуска проверить состояние процесса, повторить безопасный `getMe`, пройти `/start` и открыть `🥗 Дневник питания` в личном чате.
9. Отдельно проверить ручной ввод, согласие перед фото, ошибку AI с переходом к ручному пути, подтверждение без дубля, день и неделю, смену часового пояса, привязку тренера и запрет доступа к чужому клиенту.

## Проверка панели

Панель включается только после настройки HTTPS origin и allowlist. После этого нужно проверить подпись и срок Telegram `initData`, доступ только к привязанным клиентам, изменение норм и записи, комментарий тренера и безопасный вывод пользовательского текста. Локальный HTTP-ответ сам по себе не подтверждает готовность Mini App.

## Откат

При ошибке остановить штатный процесс, вернуть сохранённую версию файлов и прежний commit штатным способом проекта, восстановить прежнюю конфигурацию и снова запустить сервис. После отката проверить процесс и `getMe`.

Живую SQLite при откате кода не удалять. Восстанавливать данные из резервной копии только при подтверждённом повреждении или несовместимости. Перед восстановлением остановить приложение и сохранить проблемную базу вместе с её WAL/SHM отдельно для разбора. Самодостаточную копию, созданную через SQLite backup API, не смешивать с WAL/SHM другой версии базы.

Состав пакета, базовый commit, размеры и SHA-256 файлов записаны в `staging/nutrition-release-20260907/manifest.json` рядом с архивом.

## Живой выпуск 7 сентября 2026

Пакет опубликован commit `32dea40266b9c155d09b5ca80ae8970546d815ff`. Штатный GitHub Actions run `34157950131` завершился со статусом `success`. Production checkout перешёл на тот же commit, systemd-сервис `natrium-smm-bot` активен без автоматических перезапусков.

Перед публикацией создана резервная копия `nutrition-20260907T200149Z`: каталог имеет права `700`, архив кода, commit и копия конфигурации имеют права `600`. До выпуска nutrition SQLite не существовала, поэтому копия данных не требовалась.

После запуска подтверждены `getMe` для `natrium_smm_bot`, отдельная reply-кнопка `🥗 Дневник питания`, команды `/nutrition`, `/today`, `/week`, callback и message handlers. SQLite прошла `PRAGMA integrity_check`, содержит 11 обязательных таблиц и версию схемы 1. Панель выключена через `NUTRITION_DASHBOARD_ENABLED=0`.

Установленный OpenAI SDK версии 3.8.0 принимает используемые параметры Responses API `input`, `store` и `text`. Один синтетический запрос с описанием еды прошёл через production-адаптер и вернул валидный структурированный результат. Реальные Telegram-сообщения пользователям не отправлялись. Полный интерактивный проход через личный чат остаётся ручной проверкой владельца.

После выпуска владелец явно выбран первым тренером. Проверенный Telegram ID добавлен единственным значением в `TRAINER_TELEGRAM_IDS`; после одного перезапуска сервис активен, а production-проверка `is_trainer` вернула `true`. `ADMIN_TELEGRAM_ID` не менялся. Приглашение и тестовые записи от имени владельца автоматически не создавались.

## Подготовленный следующий выпуск 8 сентября 2026

Локально подготовлено расширение схемы v1 в v2: справочный расчёт USDA со снимком значений на 100 г, постоянный заменяемый код тренера, пошаговые нормы, атомарный XLSX-план и навигация комментариев без технических ID. Старые одноразовые коды остаются одноразовыми и сохраняют погашенное или истёкшее состояние. Старые записи не пересчитываются: фото-оценки без ручной правки отмечаются как AI, остальные записи с невосстановимым происхождением как legacy.

Локальная приёмка на временных базах завершена: `143 passed`. Проверены откат прерванной миграции, снимок всех исходных строк заполненной v1 базы, целостность внешних ключей, конкурентные коды, пересчёт справочной массы, отмена и устаревшие callback, смена клиента, `/start`, `/help`, XLSX без частичной записи и привязка подтверждения к клиенту и случайной сессии.

Этот раздел описывает подготовленный код, а не production-состояние. До публикации нужны свежая резервная копия кода и конфигурации, согласованная копия действующей SQLite через backup API, фиксация контрольных counts без персональных данных, штатный deployment и проверка миграции, service status, commit SHA, `getMe` и безопасной выборки ошибок. Живые пользовательские записи и сообщения для проверки создавать не нужно.
