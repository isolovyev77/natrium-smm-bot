# Руководство по обновлению системного промпта агента

## Проблема

Агент Yandex Cloud использует **старую версию** системного промпта, которая хранится на стороне Yandex Cloud. Изменения в файле [prompts/agent_system_prompt.md](prompts/agent_system_prompt.md) **не применяются автоматически** к агенту.

### Симптомы устаревшего промпта:

1. ❌ Посты начинаются без заголовка (сразу с секции типа "🔥 ПОЛЬЗА...")
2. ❌ Отсутствует лид-затравка после заголовка
3. ❌ Некорректная обработка источников ([Исследования] вместо (Исследования))

## Решение: Обновление промпта в Yandex Cloud

### Вариант 1: Через Telegram команду (РЕКОМЕНДУЕТСЯ)

1. **Добавьте свой Telegram ID в `.env`:**
   ```bash
   ADMIN_TELEGRAM_ID=123456789  # Ваш реальный Telegram ID
   ```

2. **Перезапустите бота:**
   ```bash
   sudo systemctl restart natrium-smm-bot
   ```

3. **Отправьте команду в Telegram:**
   ```
   /update_prompt
   ```

4. **Дождитесь подтверждения:**
   ```
   ✅ Системный промпт успешно обновлён!
   
   Агент Yandex Cloud теперь использует новые требования:
   • Обязательный заголовок в CAPS
   • Обязательный лид-затравка после заголовка
   • Усиленный контроль структуры постов
   ```

### Вариант 2: Через Python скрипт

Если нет доступа к Telegram боту:

```python
from src.bot import NatriumBot

bot = NatriumBot()
success = bot.update_agent_prompt("agent_system_prompt.md")

if success:
    print("✅ Промпт обновлён!")
else:
    print("❌ Ошибка обновления")
```

### Вариант 3: Через Yandex Cloud Console (вручную)

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Перейдите в раздел **AI Foundation Models** → **Agents**
3. Найдите агента `Natrium SMM Bot`
4. Нажмите **Edit** (редактировать)
5. Скопируйте содержимое [prompts/agent_system_prompt.md](prompts/agent_system_prompt.md)
6. Вставьте в поле **System Prompt**
7. Сохраните изменения

## Проверка обновления

После обновления промпта проверьте генерацию поста:

### ✅ Правильная структура:

```
💪 **ГРЕБЛЯ CONCEPT2: КАЛОРИИ ГОРЯТ КАК НИКОГДА**

Часто слышу: "Гребля — это скучно!" А что, если я скажу, что за 30 минут можно сжечь 400 ккал?

🔥 **ПОЛЬЗА ГРЕБЛИ:**
• развивает все группы мышц CrossFit
• улучшает выносливость (PubMed)
...
```

### ❌ Неправильная структура (устаревший промпт):

```
🔥 ПОЛЬЗА ГРЕБЛИ:
• развивает все группы мышц CrossFit
• улучшает выносливость PubMed
...
```

## История изменений промпта

| Дата | Изменение | Коммит |
|------|-----------|--------|
| 30.01.2026 | Обязательный заголовок + лид, обработка источников | [50d39a0](https://github.com/isolovyev77/natrium-smm-bot/commit/50d39a0) |
| 25.01.2026 | Структура постов, кнопка регенерации тем | [8a5d8cc](https://github.com/isolovyev77/natrium-smm-bot/commit/8a5d8cc) |

## Важно!

⚠️ **Обновление промпта НЕ затрагивает уже сгенерированные посты**. Оно влияет только на **новые** запросы к агенту.

⚠️ **Изменения в agent_system_prompt.md вступают в силу только после обновления через API или Console**.

## Дополнительная информация

- **API endpoint:** `PATCH https://rest-assistant.api.cloud.yandex.net/v1/agents/{agent_id}`
- **Документация:** [Yandex Cloud AI Foundation Models](https://cloud.yandex.ru/docs/foundation-models/)
- **Код функции:** [src/bot.py:update_agent_prompt()](src/bot.py)
