# 🔒 Защита от множественного запуска

## Проблема

Telegram Bot API позволяет только **одному экземпляру** бота получать обновления одновременно. При попытке запуска второго экземпляра возникает ошибка:

```
telegram.error.Conflict: Conflict: terminated by other getUpdates request; 
make sure that only one bot instance is running
```

Это может произойти если:
- ✅ Systemd service запущен
- ✅ Вы запускаете бота вручную для тестирования
- ✅ CI/CD запускает деплой пока старый процесс еще активен

## Решение

Реализована **file-based блокировка** с использованием `fcntl.flock()`:

### Как работает

1. **При запуске** бот пытается получить эксклюзивную блокировку файла `/tmp/natrium-smm-bot.pid`
2. **Если блокировка успешна:**
   - Записывается PID текущего процесса
   - Бот запускается нормально
3. **Если блокировка занята:**
   - Бот показывает сообщение с PID уже запущенного процесса
   - Завершается с кодом ошибки 1
4. **При выходе** блокировка автоматически освобождается

### Преимущества

✅ **Атомарная операция** - гарантия на уровне ядра Linux  
✅ **Автоматическая очистка** - блокировка освобождается даже при crash  
✅ **Информативные сообщения** - показывает PID конфликтующего процесса  
✅ **Работает везде** - systemd, ручной запуск, CI/CD  

## Использование

### Обычный запуск

```bash
# Запуск через systemd (рекомендуется)
sudo systemctl start natrium-smm-bot

# Ручной запуск (для тестирования)
cd /opt/natrium-smm-bot
source venv/bin/activate
python3 src/telegram_bot.py
```

### Если бот уже запущен

```bash
# Попытка второго запуска покажет:
❌ ОШИБКА: Другой экземпляр natrium-smm-bot уже запущен!
   PID запущенного процесса: 12345

Чтобы остановить его, выполните:
   sudo systemctl stop natrium-smm-bot
   или: kill 12345
```

### Остановка бота

```bash
# Через systemd
sudo systemctl stop natrium-smm-bot

# Или напрямую (если запущен вручную)
# Ctrl+C в терминале где запущен бот

# Или через kill
pkill -f telegram_bot.py
```

## Диагностика

### Проверить запущен ли бот

```bash
# Через systemd
sudo systemctl status natrium-smm-bot

# Через процессы
ps aux | grep telegram_bot.py

# Проверить PID файл
cat /tmp/natrium-smm-bot.pid
```

### Проверить блокировку

```bash
# Если файл существует и процесс жив - блокировка активна
if [ -f /tmp/natrium-smm-bot.pid ]; then
    PID=$(cat /tmp/natrium-smm-bot.pid)
    if ps -p $PID > /dev/null; then
        echo "✅ Бот запущен, PID: $PID"
    else
        echo "⚠️ PID файл есть, но процесс не найден (zombie lock)"
        rm /tmp/natrium-smm-bot.pid
    fi
else
    echo "❌ Бот не запущен"
fi
```

### Принудительная очистка (если что-то пошло не так)

```bash
# Убить все процессы бота
sudo pkill -9 -f telegram_bot.py

# Удалить PID файл
sudo rm -f /tmp/natrium-smm-bot.pid

# Перезапустить
sudo systemctl restart natrium-smm-bot
```

## Технические детали

### Код блокировки

```python
import fcntl

# Получение блокировки (неблокирующий режим)
LOCK_FILE = open("/tmp/natrium-smm-bot.pid", 'w')
fcntl.flock(LOCK_FILE.fileno(), fcntl.LOCK_EX | fcntl.LOCK_NB)
```

**Параметры:**
- `LOCK_EX` - эксклюзивная блокировка (только один процесс)
- `LOCK_NB` - неблокирующий режим (fail fast если занято)

### Автоматическая очистка

```python
import atexit

def release_lock():
    fcntl.flock(LOCK_FILE.fileno(), fcntl.LOCK_UN)
    LOCK_FILE.close()
    os.unlink("/tmp/natrium-smm-bot.pid")

# Вызывается автоматически при выходе
atexit.register(release_lock)
```

### Гарантии безопасности

1. **Блокировка на уровне ядра** - гарантируется Linux kernel
2. **Автоочистка при crash** - kernel освобождает блокировку при завершении процесса
3. **Атомарность** - невозможна race condition между проверкой и установкой блокировки

## CI/CD интеграция

GitHub Actions workflow автоматически:

1. Останавливает старый процесс: `systemctl stop natrium-smm-bot`
2. Обновляет код: `git pull`
3. Запускает новый процесс: `systemctl start natrium-smm-bot`

Блокировка предотвращает случайные конфликты если:
- Процесс не успел остановиться
- Вы тестируете вручную во время деплоя
- Запущено несколько CI/CD джобов одновременно

## Troubleshooting

### Бот падает с "Conflict" ошибкой

```bash
# Проверьте что нет других экземпляров
ps aux | grep telegram_bot.py

# Убейте все найденные процессы
sudo pkill -f telegram_bot.py

# Проверьте PID файл
rm -f /tmp/natrium-smm-bot.pid

# Перезапустите
sudo systemctl restart natrium-smm-bot
```

### PID файл остался после crash

```bash
# Проверьте существует ли процесс
cat /tmp/natrium-smm-bot.pid
ps -p <PID>

# Если процесса нет - удалите файл
sudo rm /tmp/natrium-smm-bot.pid
```

### Блокировка не работает

```bash
# Проверьте что файл создается в /tmp
ls -la /tmp/natrium-smm-bot.pid

# Проверьте права доступа
sudo chmod 666 /tmp/natrium-smm-bot.pid

# Проверьте логи
sudo journalctl -u natrium-smm-bot -f
```

## Альтернативные подходы

### ❌ Проверка через `ps` (ненадежно)

```bash
# НЕ используйте - есть race conditions
if ps aux | grep -v grep | grep telegram_bot.py; then
    echo "Already running"
    exit 1
fi
```

**Проблемы:**
- Race condition между проверкой и запуском
- Может найти другие процессы (grep сам себя, старые zombie процессы)

### ✅ fcntl блокировка (используется)

- Атомарная операция
- Гарантируется kernel
- Автоматическая очистка

### ✅ systemd (дополнительная защита)

```ini
# В natrium-smm-bot.service уже есть:
Restart=always
RestartSec=10
```

Systemd сам следит что запущен только один процесс.

## Best Practices

1. **Production:** Всегда используйте `systemctl start/stop`
2. **Development:** Сначала проверяйте `systemctl status`
3. **CI/CD:** Всегда останавливайте перед обновлением
4. **Debugging:** Используйте `journalctl -f` для просмотра логов

## См. также

- [DEPLOY_DEBUG.md](DEPLOY_DEBUG.md) - Отладка проблем деплоя
- [VM_PRODUCTION_SETUP.md](VM_PRODUCTION_SETUP.md) - Настройка production окружения
- [DEPLOYMENT.md](DEPLOYMENT.md) - Процесс деплоя через CI/CD
