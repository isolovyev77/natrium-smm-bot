# HTML форматирование в Telegram боте

## Обзор изменений

Telegram бот теперь использует HTML-форматирование вместо Markdown для корректного отображения кликабельных ссылок из постов.

## Причина изменений

Яндекс GPT генерирует ссылки в формате Markdown: `[текст](URL)`

Для корректного отображения кликабельных ссылок в Telegram нужно использовать HTML формат: `<a href="URL">текст</a>`

## Что изменилось

### 1. Новая функция конвертации `convert_markdown_to_html()`

**Расположение:** `src/telegram_bot.py`

Функция автоматически конвертирует:
- `[текст](URL)` → `<a href="URL">текст</a>` (кликабельные ссылки)
- `**текст**` → `<b>текст</b>` (жирный текст)
- `*текст*` → `<i>текст</i>` (курсив, но не буллеты)

**Пример использования:**
```python
post = convert_markdown_to_html(post)
await query.message.reply_text(post, parse_mode='HTML')
```

### 2. Изменён parse_mode во всех сообщениях

Было: `parse_mode='Markdown'`
Стало: `parse_mode='HTML'`

Затронуты:
- ✅ Все `reply_text()` и `edit_message_text()` вызовы
- ✅ Форматирование статистики токенов
- ✅ Форматирование меню и настроек

### 3. Обновлено форматирование текстов

#### Было (Markdown):
```python
text = f"**{title}**"
text = f"*курсив*"
```

#### Стало (HTML):
```python
text = f"<b>{title}</b>"
text = f"<i>курсив</i>"
```

### 4. Удалена конвертация `**` → `*`

Старый код для Markdown:
```python
# Удалено: больше не нужно
post = post.replace('**', '*')
```

Новый подход - полная конвертация в HTML через `convert_markdown_to_html()`.

## Поддерживаемые HTML теги

Telegram поддерживает только базовые HTML теги:

| Тег | Описание | Пример |
|-----|----------|--------|
| `<a href="URL">текст</a>` | Ссылки | `<a href="https://pubmed.ncbi.nlm.nih.gov/123/">PubMed</a>` |
| `<b>текст</b>` | Жирный | `<b>РАЗМИНКА</b>` |
| `<i>текст</i>` | Курсив | `<i>важно</i>` |
| `<u>текст</u>` | Подчёркивание | `<u>внимание</u>` |
| `<s>текст</s>` | Зачёркивание | `<s>старое</s>` |

## Примеры конвертации

### Простая ссылка
**Вход:** `[PubMed](https://pubmed.ncbi.nlm.nih.gov/12345678/)`  
**Выход:** `<a href="https://pubmed.ncbi.nlm.nih.gov/12345678/">PubMed</a>`

### Заголовок поста
**Вход:** `💪 **РАЗМИНКА: КЛЮЧ К РЕЗУЛЬТАТУ**`  
**Выход:** `💪 <b>РАЗМИНКА: КЛЮЧ К РЕЗУЛЬТАТУ</b>`

### Буллет со ссылкой
**Вход:** `• улучшает пищеварение [PubMed](https://pubmed.ncbi.nlm.nih.gov/12345678/)`  
**Выход:** `• улучшает пищеварение <a href="https://pubmed.ncbi.nlm.nih.gov/12345678/">PubMed</a>`

### Полный пост
```markdown
💪 **РАЗМИНКА: КЛЮЧ К РЕЗУЛЬТАТУ**

Представьте: вы приходите в зал, и первые 5 минут определяют всю тренировку.

🔥 **НАУКА**
• повышает температуру мышц на 2-3°C [PubMed](https://pubmed.ncbi.nlm.nih.gov/12345678/)
• улучшает нейромышечную проводимость (ВОЗ)

#натриумфитнес #разминка
```

Конвертируется в:
```html
💪 <b>РАЗМИНКА: КЛЮЧ К РЕЗУЛЬТАТУ</b>

Представьте: вы приходите в зал, и первые 5 минут определяют всю тренировку.

🔥 <b>НАУКА</b>
• повышает температуру мышц на 2-3°C <a href="https://pubmed.ncbi.nlm.nih.gov/12345678/">PubMed</a>
• улучшает нейромышечную проводимость (ВОЗ)

#натриумфитнес #разминка
```

## Безопасность

### Валидация URL
Убедитесь что URL:
- Начинаются с `http://` или `https://`
- Не содержат пробелов или некорректных символов
- Не содержат вредоносного кода

### Экранирование спецсимволов
HTML-теги в тексте (кроме разрешённых) игнорируются Telegram автоматически.

## Тестирование

Запустите тесты конвертации:
```bash
python test_html_conversion.py
```

Все тесты должны пройти успешно (✅).

## Файлы изменены

- ✅ `src/telegram_bot.py` - основной модуль с изменениями
- ✅ `test_html_conversion.py` - тесты конвертации
- ✅ `HTML_FORMATTING.md` - эта документация

## Совместимость

- ✅ Telegram Bot API полностью поддерживает HTML
- ✅ Все кликабельные ссылки работают корректно
- ✅ Форматирование (жирный, курсив) сохраняется
- ✅ Обратная совместимость с существующими сообщениями

## Дополнительные ресурсы

- [Telegram Bot API: Formatting](https://core.telegram.org/bots/api#formatting-options)
- [HTML-разметка в Telegram](https://core.telegram.org/bots/api#html-style)
