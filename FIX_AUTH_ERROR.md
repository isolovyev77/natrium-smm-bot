# ⚠️ ИСПРАВЛЕНИЕ ОШИБКИ АУТЕНТИФИКАЦИИ

## ❌ Ваша ошибка:

Вы использовали **имя от Google аккаунта** вместо **GitHub username**.

---

## ✅ ПРАВИЛЬНЫЕ ДАННЫЕ ДЛЯ ВХОДА:

### Username (имя пользователя):
```
isolovyev77
```
☝️ Это ваш **GitHub username** (видно в URL: github.com/isolovyev77)

### Password (пароль):
```
[Вставьте Personal Access Token]
```
☝️ Это **НЕ пароль Google!** Это токен формата `ghp_xxxxxxxxxxxx...`

---

## 🔐 Если у вас ещё нет токена:

### 1. Создайте Personal Access Token:

Откройте в браузере: **https://github.com/settings/tokens/new**

Заполните форму:
- **Note**: `natrium-smm-bot-push`
- **Expiration**: `90 days`
- **Select scopes**: 
  - ✅ `repo` (все подпункты)
  
Нажмите **"Generate token"** внизу страницы.

### 2. Скопируйте токен:

Появится строка типа:
```
ghp_abcdefghijklmnopqrstuvwxyz1234567890
```

⚠️ **СКОПИРУЙТЕ ЕГО СРАЗУ!** После обновления страницы он больше не появится.

---

## 🚀 ПРАВИЛЬНАЯ КОМАНДА ДЛЯ PUSH:

```bash
cd /Users/isolovyev/Projects/smm_bot/NatriumSMM
git push -u origin main
```

Когда появится запрос, введите:

```
Username for 'https://github.com': isolovyev77
Password for 'https://isolovyev77@github.com': [вставьте токен]
```

### Пример диалога в терминале:

```
$ git push -u origin main
Username for 'https://github.com': isolovyev77
Password for 'https://isolovyev77@github.com': ghp_abcd1234efgh5678ijkl9012mnop3456qrst7890
Enumerating objects: 89, done.
Counting objects: 100% (89/89), done.
...
To https://github.com/isolovyev77/natrium-smm-bot.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

✅ Если видите такой вывод — всё успешно!

---

## 💾 Токен сохранится автоматически

После первого успешного push macOS сохранит токен в Keychain. В следующий раз пароль запрашиваться не будет.

---

## 🔄 Если токен уже вводили неправильно

Удалите старые данные из Keychain:

```bash
# Откройте приложение Keychain Access (Связка ключей)
# Найдите: github.com
# Удалите запись
# Попробуйте push снова
```

Или используйте команду:
```bash
git credential-osxkeychain erase
host=github.com
protocol=https
```

Затем попробуйте push снова с правильными данными.

---

## ✅ ИТОГО:

| Что вводить | Правильное значение |
|-------------|---------------------|
| **Username** | `isolovyev77` |
| **Password** | Personal Access Token (начинается с `ghp_`) |

---

**Попробуйте сейчас:**

1. Создайте токен (если нет): https://github.com/settings/tokens/new
2. Скопируйте токен
3. Запустите: `git push -u origin main`
4. Username: `isolovyev77`
5. Password: вставьте токен

Всё должно заработать! 🚀
